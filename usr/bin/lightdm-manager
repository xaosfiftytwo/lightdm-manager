#!/bin/bash
echo $USER > /tmp/user
if [ $UID -eq 0 ]; then
  /usr/bin/python3 /usr/lib/lightdm-manager/lightdm-manager.py $*
else
  LAUNCHER="gksudo"
  if [ -e "/usr/bin/kdesudo" ]; then
    LAUNCHER="kdesudo"
  fi
  $LAUNCHER /usr/bin/python3 /usr/lib/lightdm-manager/lightdm-manager.py $*
fi
